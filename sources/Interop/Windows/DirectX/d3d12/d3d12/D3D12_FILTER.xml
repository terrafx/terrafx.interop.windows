<?xml version="1.0" encoding="utf-8"?>
<!-- Copyright © Tanner Gooding and Contributors. Licensed under the MIT License (MIT). See License.md in the repository root for more information. -->
<!-- Ported from https://github.com/MicrosoftDocs/sdk-api/ -->
<doc>
  <member name="D3D12_FILTER">
    <summary>Specifies filtering options during texture sampling.</summary>
  </member>
  <member name="D3D12_FILTER.D3D12_FILTER_MIN_MAG_MIP_POINT">
    <summary>Use point sampling for minification, magnification, and mip-level sampling.</summary>
  </member>
  <member name="D3D12_FILTER.D3D12_FILTER_MIN_MAG_POINT_MIP_LINEAR">
    <summary>Use point sampling for minification and magnification; use linear interpolation for mip-level sampling.</summary>
  </member>
  <member name="D3D12_FILTER.D3D12_FILTER_MIN_POINT_MAG_LINEAR_MIP_POINT">
    <summary>Use point sampling for minification; use linear interpolation for magnification; use point sampling for mip-level sampling.</summary>
  </member>
  <member name="D3D12_FILTER.D3D12_FILTER_MIN_POINT_MAG_MIP_LINEAR">
    <summary>Use point sampling for minification; use linear interpolation for magnification and mip-level sampling.</summary>
  </member>
  <member name="D3D12_FILTER.D3D12_FILTER_MIN_LINEAR_MAG_MIP_POINT">
    <summary>Use linear interpolation for minification; use point sampling for magnification and mip-level sampling.</summary>
  </member>
  <member name="D3D12_FILTER.D3D12_FILTER_MIN_LINEAR_MAG_POINT_MIP_LINEAR">
    <summary>Use linear interpolation for minification; use point sampling for magnification; use linear interpolation for mip-level sampling.</summary>
  </member>
  <member name="D3D12_FILTER.D3D12_FILTER_MIN_MAG_LINEAR_MIP_POINT">
    <summary>Use linear interpolation for minification and magnification; use point sampling for mip-level sampling.</summary>
  </member>
  <member name="D3D12_FILTER.D3D12_FILTER_MIN_MAG_MIP_LINEAR">
    <summary>Use linear interpolation for minification, magnification, and mip-level sampling.</summary>
  </member>
  <member name="D3D12_FILTER.D3D12_FILTER_ANISOTROPIC">
    <summary>Use anisotropic interpolation for minification, magnification, and mip-level sampling.</summary>
  </member>
  <member name="D3D12_FILTER.D3D12_FILTER_COMPARISON_MIN_MAG_MIP_POINT">
    <summary>Use point sampling for minification, magnification, and mip-level sampling. Compare the result to the comparison value.</summary>
  </member>
  <member name="D3D12_FILTER.D3D12_FILTER_COMPARISON_MIN_MAG_POINT_MIP_LINEAR">
    <summary>Use point sampling for minification and magnification; use linear interpolation for mip-level sampling. Compare the result to the comparison value.</summary>
  </member>
  <member name="D3D12_FILTER.D3D12_FILTER_COMPARISON_MIN_POINT_MAG_LINEAR_MIP_POINT">
    <summary>Use point sampling for minification; use linear interpolation for magnification; use point sampling for mip-level sampling. Compare the result to the comparison value.</summary>
  </member>
  <member name="D3D12_FILTER.D3D12_FILTER_COMPARISON_MIN_POINT_MAG_MIP_LINEAR">
    <summary>Use point sampling for minification; use linear interpolation for magnification and mip-level sampling. Compare the result to the comparison value.</summary>
  </member>
  <member name="D3D12_FILTER.D3D12_FILTER_COMPARISON_MIN_LINEAR_MAG_MIP_POINT">
    <summary>Use linear interpolation for minification; use point sampling for magnification and mip-level sampling. Compare the result to the comparison value.</summary>
  </member>
  <member name="D3D12_FILTER.D3D12_FILTER_COMPARISON_MIN_LINEAR_MAG_POINT_MIP_LINEAR">
    <summary>Use linear interpolation for minification; use point sampling for magnification; use linear interpolation for mip-level sampling. Compare the result to the comparison value.</summary>
  </member>
  <member name="D3D12_FILTER.D3D12_FILTER_COMPARISON_MIN_MAG_LINEAR_MIP_POINT">
    <summary>Use linear interpolation for minification and magnification; use point sampling for mip-level sampling. Compare the result to the comparison value.</summary>
  </member>
  <member name="D3D12_FILTER.D3D12_FILTER_COMPARISON_MIN_MAG_MIP_LINEAR">
    <summary>Use linear interpolation for minification, magnification, and mip-level sampling. Compare the result to the comparison value.</summary>
  </member>
  <member name="D3D12_FILTER.D3D12_FILTER_COMPARISON_ANISOTROPIC">
    <summary>Use anisotropic interpolation for minification, magnification, and mip-level sampling. Compare the result to the comparison value.</summary>
  </member>
  <member name="D3D12_FILTER.D3D12_FILTER_MINIMUM_MIN_MAG_MIP_POINT">
    <summary>Fetch the same set of texels as <see cref="D3D12_FILTER_MIN_MAG_MIP_POINT" /> and instead of filtering them return the minimum of the texels. Texels that are weighted 0 during filtering aren't counted towards the minimum. You can query support for this filter type from the <b>MinMaxFiltering</b> member in the <see cref="D3D11_FEATURE_DATA_D3D11_OPTIONS1" /> structure.</summary>
  </member>
  <member name="D3D12_FILTER.D3D12_FILTER_MINIMUM_MIN_MAG_POINT_MIP_LINEAR">
    <summary>Fetch the same set of texels as <see cref="D3D12_FILTER_MIN_MAG_POINT_MIP_LINEAR" /> and instead of filtering them return the minimum of the texels. Texels that are weighted 0 during filtering aren't counted towards the minimum. You can query support for this filter type from the <b>MinMaxFiltering</b> member in the <see cref="D3D11_FEATURE_DATA_D3D11_OPTIONS1" /> structure.</summary>
  </member>
  <member name="D3D12_FILTER.D3D12_FILTER_MINIMUM_MIN_POINT_MAG_LINEAR_MIP_POINT">
    <summary>Fetch the same set of texels as <see cref="D3D12_FILTER_MIN_POINT_MAG_LINEAR_MIP_POINT" /> and instead of filtering them return the minimum of the texels. Texels that are weighted 0 during filtering aren't counted towards the minimum. You can query support for this filter type from the <b>MinMaxFiltering</b> member in the <see cref="D3D11_FEATURE_DATA_D3D11_OPTIONS1" /> structure.</summary>
  </member>
  <member name="D3D12_FILTER.D3D12_FILTER_MINIMUM_MIN_POINT_MAG_MIP_LINEAR">
    <summary>Fetch the same set of texels as <see cref="D3D12_FILTER_MIN_POINT_MAG_MIP_LINEAR" /> and instead of filtering them return the minimum of the texels. Texels that are weighted 0 during filtering aren't counted towards the minimum. You can query support for this filter type from the <b>MinMaxFiltering</b> member in the <see cref="D3D11_FEATURE_DATA_D3D11_OPTIONS1" /> structure.</summary>
  </member>
  <member name="D3D12_FILTER.D3D12_FILTER_MINIMUM_MIN_LINEAR_MAG_MIP_POINT">
    <summary>Fetch the same set of texels as <see cref="D3D12_FILTER_MIN_LINEAR_MAG_MIP_POINT" /> and instead of filtering them return the minimum of the texels. Texels that are weighted 0 during filtering aren't counted towards the minimum. You can query support for this filter type from the <b>MinMaxFiltering</b> member in the <see cref="D3D11_FEATURE_DATA_D3D11_OPTIONS1" /> structure.</summary>
  </member>
  <member name="D3D12_FILTER.D3D12_FILTER_MINIMUM_MIN_LINEAR_MAG_POINT_MIP_LINEAR">
    <summary>Fetch the same set of texels as <see cref="D3D12_FILTER_MIN_LINEAR_MAG_POINT_MIP_LINEAR" /> and instead of filtering them return the minimum of the texels. Texels that are weighted 0 during filtering aren't counted towards the minimum. You can query support for this filter type from the <b>MinMaxFiltering</b> member in the <see cref="D3D11_FEATURE_DATA_D3D11_OPTIONS1" /> structure.</summary>
  </member>
  <member name="D3D12_FILTER.D3D12_FILTER_MINIMUM_MIN_MAG_LINEAR_MIP_POINT">
    <summary>Fetch the same set of texels as <see cref="D3D12_FILTER_MIN_MAG_LINEAR_MIP_POINT" /> and instead of filtering them return the minimum of the texels. Texels that are weighted 0 during filtering aren't counted towards the minimum. You can query support for this filter type from the <b>MinMaxFiltering</b> member in the <see cref="D3D11_FEATURE_DATA_D3D11_OPTIONS1" /> structure.</summary>
  </member>
  <member name="D3D12_FILTER.D3D12_FILTER_MINIMUM_MIN_MAG_MIP_LINEAR">
    <summary>Fetch the same set of texels as <see cref="D3D12_FILTER_MIN_MAG_MIP_LINEAR" /> and instead of filtering them return the minimum of the texels. Texels that are weighted 0 during filtering aren't counted towards the minimum. You can query support for this filter type from the <b>MinMaxFiltering</b> member in the <see cref="D3D11_FEATURE_DATA_D3D11_OPTIONS1" /> structure.</summary>
  </member>
  <member name="D3D12_FILTER.D3D12_FILTER_MINIMUM_ANISOTROPIC">
    <summary>Fetch the same set of texels as <see cref="D3D12_FILTER_ANISOTROPIC" /> and instead of filtering them return the minimum of the texels. Texels that are weighted 0 during filtering aren't counted towards the minimum. You can query support for this filter type from the <b>MinMaxFiltering</b> member in the <see cref="D3D11_FEATURE_DATA_D3D11_OPTIONS1" /> structure.</summary>
  </member>
  <member name="D3D12_FILTER.D3D12_FILTER_MAXIMUM_MIN_MAG_MIP_POINT">
    <summary>Fetch the same set of texels as <see cref="D3D12_FILTER_MIN_MAG_MIP_POINT" /> and instead of filtering them return the maximum of the texels. Texels that are weighted 0 during filtering aren't counted towards the maximum. You can query support for this filter type from the <b>MinMaxFiltering</b> member in the <see cref="D3D11_FEATURE_DATA_D3D11_OPTIONS1" /> structure.</summary>
  </member>
  <member name="D3D12_FILTER.D3D12_FILTER_MAXIMUM_MIN_MAG_POINT_MIP_LINEAR">
    <summary>Fetch the same set of texels as <see cref="D3D12_FILTER_MIN_MAG_POINT_MIP_LINEAR" /> and instead of filtering them return the maximum of the texels. Texels that are weighted 0 during filtering aren't counted towards the maximum. You can query support for this filter type from the <b>MinMaxFiltering</b> member in the <see cref="D3D11_FEATURE_DATA_D3D11_OPTIONS1" /> structure.</summary>
  </member>
  <member name="D3D12_FILTER.D3D12_FILTER_MAXIMUM_MIN_POINT_MAG_LINEAR_MIP_POINT">
    <summary>Fetch the same set of texels as <see cref="D3D12_FILTER_MIN_POINT_MAG_LINEAR_MIP_POINT" /> and instead of filtering them return the maximum of the texels. Texels that are weighted 0 during filtering aren't counted towards the maximum. You can query support for this filter type from the <b>MinMaxFiltering</b> member in the <see cref="D3D11_FEATURE_DATA_D3D11_OPTIONS1" /> structure.</summary>
  </member>
  <member name="D3D12_FILTER.D3D12_FILTER_MAXIMUM_MIN_POINT_MAG_MIP_LINEAR">
    <summary>Fetch the same set of texels as <see cref="D3D12_FILTER_MIN_POINT_MAG_MIP_LINEAR" /> and instead of filtering them return the maximum of the texels. Texels that are weighted 0 during filtering aren't counted towards the maximum. You can query support for this filter type from the <b>MinMaxFiltering</b> member in the <see cref="D3D11_FEATURE_DATA_D3D11_OPTIONS1" /> structure.</summary>
  </member>
  <member name="D3D12_FILTER.D3D12_FILTER_MAXIMUM_MIN_LINEAR_MAG_MIP_POINT">
    <summary>Fetch the same set of texels as <see cref="D3D12_FILTER_MIN_LINEAR_MAG_MIP_POINT" /> and instead of filtering them return the maximum of the texels. Texels that are weighted 0 during filtering aren't counted towards the maximum. You can query support for this filter type from the <b>MinMaxFiltering</b> member in the <see cref="D3D11_FEATURE_DATA_D3D11_OPTIONS1" /> structure.</summary>
  </member>
  <member name="D3D12_FILTER.D3D12_FILTER_MAXIMUM_MIN_LINEAR_MAG_POINT_MIP_LINEAR">
    <summary>Fetch the same set of texels as <see cref="D3D12_FILTER_MIN_LINEAR_MAG_POINT_MIP_LINEAR" /> and instead of filtering them return the maximum of the texels. Texels that are weighted 0 during filtering aren't counted towards the maximum. You can query support for this filter type from the <b>MinMaxFiltering</b> member in the <see cref="D3D11_FEATURE_DATA_D3D11_OPTIONS1" /> structure.</summary>
  </member>
  <member name="D3D12_FILTER.D3D12_FILTER_MAXIMUM_MIN_MAG_LINEAR_MIP_POINT">
    <summary>Fetch the same set of texels as <see cref="D3D12_FILTER_MIN_MAG_LINEAR_MIP_POINT" /> and instead of filtering them return the maximum of the texels. Texels that are weighted 0 during filtering aren't counted towards the maximum. You can query support for this filter type from the <b>MinMaxFiltering</b> member in the <see cref="D3D11_FEATURE_DATA_D3D11_OPTIONS1" /> structure.</summary>
  </member>
  <member name="D3D12_FILTER.D3D12_FILTER_MAXIMUM_MIN_MAG_MIP_LINEAR">
    <summary>Fetch the same set of texels as <see cref="D3D12_FILTER_MIN_MAG_MIP_LINEAR" /> and instead of filtering them return the maximum of the texels. Texels that are weighted 0 during filtering aren't counted towards the maximum. You can query support for this filter type from the <b>MinMaxFiltering</b> member in the <see cref="D3D11_FEATURE_DATA_D3D11_OPTIONS1" /> structure.</summary>
  </member>
  <member name="D3D12_FILTER.D3D12_FILTER_MAXIMUM_ANISOTROPIC">
    <summary>Fetch the same set of texels as <see cref="D3D12_FILTER_ANISOTROPIC" /> and instead of filtering them return the maximum of the texels. Texels that are weighted 0 during filtering aren't counted towards the maximum. You can query support for this filter type from the <b>MinMaxFiltering</b> member in the <see cref="D3D11_FEATURE_DATA_D3D11_OPTIONS1" /> structure.</summary>
  </member>
</doc>